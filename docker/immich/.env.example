# Immich environment — copy to .env and fill in values
IMMICH_VERSION=release

# Upload location on the CT filesystem (NAS mount)
UPLOAD_LOCATION=/datos

# PostgreSQL data location
DB_DATA_LOCATION=/var/lib/immich/postgres

# Database credentials
DB_USERNAME=immich
DB_PASSWORD=<REDACTED>
DB_DATABASE_NAME=immich
